const translations = {
  en: {
    navHome: 'Home',
    navCalculator: 'Calculator',
    navPrices: 'Prices',
    navTournaments: 'Tournaments',
    navContact: 'Contact',
    heroSectionLabel: 'Premium gaming finance',
    heroTitle: 'Welcome to SALONI DIAMONDA.',
    heroSubtitle: 'Your premium destination for clear diamond pricing, elegant design, and fast access to the best rates in Moroccan Dirhams.',
    calculatorLabel: 'Calculator',
    calculatorTitle: 'Instant diamond totals with a premium gaming feel.',
    calculatorSubtitle: "Use SALONI DIAMONDA's calculator to convert diamonds into Moroccan Dirhams instantly. Live updates, clear formatting, and easy copy/reset controls.",
    diamondsLabel: 'Number of Diamonds',
    diamondsNote: 'Enter diamond quantity for the total estimate.',
    priceLabel: 'Price per 100 Diamonds',
    priceNote: 'Default value is 15 DH per 100.',
    reset: 'Reset',
    copyResult: 'Copy Result',
    resultLabel: 'Total in DH',
    formulaNote: 'Formula: Total = (Diamonds / 100) × Price Per 100',
    contactLabel: 'Contact',
    contactTitle: 'Need help? Contact us anytime.',
    contactSubtitle: 'Reach out for support, feature guidance, or partnership details. Our team is ready to help you with the premium SALONI DIAMONDA experience.',
    contactButton: 'Send message',
    formName: 'Name',
    formEmail: 'Email',
    formSubject: 'Subject',
    formMessage: 'Message',
    tournamentHeroLabel: 'Esports Arena',
    tournamentHeroTitle: 'Active Tournaments & Custom Rooms',
    tournamentHeroSubtitle: 'Enter your Game ID directly below to register!',
    dailyRoom: 'DAILY ROOM',
    dailyTitle: 'Solo Diamond Rush',
    dailyDesc: 'Prize Pool: 500 💎 Date: Tonight at 22:00 GMT',
    registerNow: 'Register Now',
    confirmSlots: 'Confirm Slots',
    weeklyRoom: 'WEEKLY CUP',
    weeklyTitle: 'ZARANE 100K Championship',
    weeklyDesc: 'Prize Pool: 2,500 💎 Date: Every Sunday',
    registeredPlayersTitle: 'Registered Players List',
    noPlayers: 'No players registered yet. Be the first!',
    hallOfFameTitle: 'Hall of Fame',
    hallOfFameSubtitle: 'Recent winners and standout champions.',
    rank: 'Rank',
    playerId: 'Player ID',
    tournament: 'Tournament',
    prize: 'Prize',
    status: 'Status',
    statusRegistered: 'Registered'
  },
  fr: {
    navHome: 'Accueil',
    navCalculator: 'Calculatrice',
    navPrices: 'Prix',
    navTournaments: 'Tournois',
    navContact: 'Contact',
    heroSectionLabel: 'Finance premium gaming',
    heroTitle: 'Bienvenue chez SALONI DIAMONDA.',
    heroSubtitle: 'Votre destination premium pour des prix de diamants clairs, un design élégant et un accès rapide aux meilleurs tarifs en dirhams marocains.',
    calculatorLabel: 'Calculatrice',
    calculatorTitle: 'Totaux instantanés avec une ambiance gaming premium.',
    calculatorSubtitle: "Utilisez la calculatrice SALONI DIAMONDA pour convertir les diamants en dirhams marocains instantanément. Mise à jour en direct, formatage clair et contrôles de copie/réinitialisation faciles.",
    diamondsLabel: 'Nombre de diamants',
    diamondsNote: 'Saisissez la quantité de diamants pour l’estimation.',
    priceLabel: 'Prix pour 100 diamants',
    priceNote: 'La valeur par défaut est de 15 DH pour 100.',
    reset: 'Réinitialiser',
    copyResult: 'Copier le résultat',
    resultLabel: 'Total en DH',
    formulaNote: 'Formule : Total = (Diamants / 100) × Prix pour 100',
    contactLabel: 'Contact',
    contactTitle: 'Besoin d’aide ? Contactez-nous.',
    contactSubtitle: 'Contactez-nous pour de l’aide, des conseils ou des détails de partenariat. Notre équipe est prête à vous aider.',
    contactButton: 'Envoyer le message',
    formName: 'Nom',
    formEmail: 'E-mail',
    formSubject: 'Sujet',
    formMessage: 'Message',
    tournamentHeroLabel: 'Arène Esports',
    tournamentHeroTitle: 'Tournois actifs et salles personnalisées',
    tournamentHeroSubtitle: 'Entrez votre ID de jeu ci-dessous pour vous inscrire !',
    dailyRoom: 'SALLE QUOTIDIENNE',
    dailyTitle: 'Course solo de diamants',
    dailyDesc: 'Prix : 500 💎 Date : Ce soir à 22:00 GMT',
    registerNow: 'S’inscrire',
    confirmSlots: 'Confirmer les places',
    weeklyRoom: 'COUPE HEBDOMADAIRE',
    weeklyTitle: 'Championnat ZARANE 100K',
    weeklyDesc: 'Prix : 2 500 💎 Date : Chaque dimanche',
    registeredPlayersTitle: 'Liste des joueurs inscrits',
    noPlayers: 'Aucun joueur inscrit pour le moment. Soyez le premier !',
    hallOfFameTitle: 'Temple de la renommée',
    hallOfFameSubtitle: 'Gagnants récents et champions exceptionnels.',
    rank: 'Rang',
    playerId: 'ID du joueur',
    tournament: 'Tournoi',
    prize: 'Prix',
    status: 'Statut',
    statusRegistered: 'Inscrit'
  },
  ar: {
    navHome: 'الرئيسية',
    navCalculator: 'الحاسبة',
    navPrices: 'الأسعار',
    navTournaments: 'البطولات',
    navContact: 'التواصل',
    heroSectionLabel: 'تمويل الألعاب المميز',
    heroTitle: 'مرحبًا بكم في SALONI DIAMONDA.',
    heroSubtitle: 'وجهتكم المميزة للحصول على أسعار diamantes واضحة وتصميم أنيق ووصول سريع إلى أفضل الأسعار بالدرهم المغربي.',
    calculatorLabel: 'الحاسبة',
    calculatorTitle: 'إجماليات فورية مع شعور ألعاب مميز.',
    calculatorSubtitle: 'استخدم حاسبة SALONI DIAMONDA لتحويل الأحجار الكريمة إلى دراهم مغربية فورًا. تحديث مباشر وتنسيق واضح وأزرار نسخ/إعادة تعيين سهلة.',
    diamondsLabel: 'عدد الأحجار الكريمة',
    diamondsNote: 'أدخل كمية الأحجار الكريمة للتقدير.',
    priceLabel: 'السعر لكل 100 حجر كريم',
    priceNote: 'القيمة الافتراضية هي 15 درهم لكل 100.',
    reset: 'إعادة تعيين',
    copyResult: 'نسخ النتيجة',
    resultLabel: 'الإجمالي بالدرهم',
    formulaNote: 'الصيغة: الإجمالي = (الأحجار الكريمة / 100) × السعر لكل 100',
    contactLabel: 'التواصل',
    contactTitle: 'هل تحتاج مساعدة؟ تواصل معنا في أي وقت.',
    contactSubtitle: 'تواصل معنا للدعم أو الإرشاد أو تفاصيل الشراكة. فريقنا جاهز لمساعدتك.',
    contactButton: 'إرسال الرسالة',
    formName: 'الاسم',
    formEmail: 'البريد الإلكتروني',
    formSubject: 'الموضوع',
    formMessage: 'الرسالة',
    tournamentHeroLabel: 'ساحة الألعاب الإلكترونية',
    tournamentHeroTitle: 'بطولات نشطة وغرف مخصصة',
    tournamentHeroSubtitle: 'أدخل معرف اللعبة أدناه للتسجيل!',
    dailyRoom: 'غرفة يومية',
    dailyTitle: 'سباق الأحجار الكريمة الفردي',
    dailyDesc: 'المجموعة: 500 💎 التاريخ: الليلة عند 22:00 بتوقيت غرينتش',
    registerNow: 'تسجيل الآن',
    confirmSlots: 'تأكيد المقاعد',
    weeklyRoom: 'كأس أسبوعي',
    weeklyTitle: 'بطولة ZARANE 100K',
    weeklyDesc: 'المجموعة: 2,500 💎 التاريخ: كل يوم أحد',
    registeredPlayersTitle: 'قائمة اللاعبين المسجلين',
    noPlayers: 'لم يتم تسجيل أي لاعب بعد. كن أول من يفعل!',
    hallOfFameTitle: 'قاعة الشرف',
    hallOfFameSubtitle: 'الفائزون الأخيرون والأبطال المميزون.',
    rank: 'الترتيب',
    playerId: 'معرّف اللاعب',
    tournament: 'البطولة',
    prize: 'الجائزة',
    status: 'الحالة',
    statusRegistered: 'مسجل'
  }
};

function getTranslation(lang, key) {
  return translations[lang]?.[key] || translations.en[key] || '';
}

function applyTranslations() {
  const savedLang = localStorage.getItem('preferredLanguage') || 'en';
  const lang = translations[savedLang] ? savedLang : 'en';
  const t = translations[lang];
  document.documentElement.lang = lang;
  document.querySelectorAll('[data-i18n]').forEach(element => {
    const key = element.getAttribute('data-i18n');
    if (t[key]) {
      if (element.tagName === 'INPUT' || element.tagName === 'TEXTAREA') {
        element.placeholder = t[key];
      } else {
        element.textContent = t[key];
      }
    }
  });
  document.querySelectorAll('[data-i18n-placeholder]').forEach(element => {
    const key = element.getAttribute('data-i18n-placeholder');
    if (t[key]) {
      element.placeholder = t[key];
    }
  });

  const langSelect = document.querySelector('.lang-switcher select');
  if (langSelect) {
    langSelect.value = lang;
  }

  const copyBtn = document.getElementById('copyBtn');
  if (copyBtn) {
    copyBtn.textContent = t.copyResult || 'Copy Result';
  }

  const resetBtn = document.getElementById('resetBtn');
  if (resetBtn) {
    resetBtn.textContent = t.reset || 'Reset';
  }

  const submitButtons = document.querySelectorAll('button[type="submit"]');
  submitButtons.forEach(button => {
    const key = button.getAttribute('data-i18n');
    if (key && t[key]) {
      button.textContent = t[key];
    }
  });

  if (typeof renderHallOfFame === 'function') {
    renderHallOfFame();
  }
}

document.addEventListener('DOMContentLoaded', () => {
  const navToggle = document.querySelector('.nav-toggle');
  const navMenu = document.querySelector('.main-nav');
  const navLinks = document.querySelectorAll('.main-nav a');
  const diamondsInput = document.getElementById('diamonds');
  const priceInput = document.getElementById('price');
  const resultEl = document.getElementById('result');
  const resultBox = document.getElementById('resultBox');
  const errorMessage = document.getElementById('errorMessage');
  const copyBtn = document.getElementById('copyBtn');
  const resetBtn = document.getElementById('resetBtn');
  const contactForm = document.querySelector('.contact-form form');
  const newsletterForms = document.querySelectorAll('.newsletter-form');

  if (navToggle && navMenu) {
    navToggle.addEventListener('click', () => {
      navMenu.classList.toggle('show');
    });
  }

  navLinks.forEach(link => {
    link.addEventListener('click', () => {
      navMenu.classList.remove('show');
    });
  });

  if (diamondsInput && priceInput && resultEl && resultBox) {
    const handleInput = () => calculateLive(diamondsInput, priceInput, resultEl, resultBox, errorMessage);
    diamondsInput.addEventListener('input', handleInput);
    priceInput.addEventListener('input', handleInput);
    handleInput();
  }

  if (copyBtn) {
    copyBtn.addEventListener('click', async () => {
      if (resultEl) {
        const currentLang = localStorage.getItem('preferredLanguage') || 'en';
        const t = translations[currentLang] || translations.en;
        try {
          await navigator.clipboard.writeText(resultEl.textContent || '');
          copyBtn.textContent = t.copied || 'Copied!';
          setTimeout(() => { copyBtn.textContent = t.copyResult || 'Copy Result'; }, 1600);
        } catch (error) {
          copyBtn.textContent = t.copyFailed || 'Copy failed';
          setTimeout(() => { copyBtn.textContent = t.copyResult || 'Copy Result'; }, 1600);
        }
      }
    });
  }

  if (resetBtn && diamondsInput && priceInput) {
    resetBtn.addEventListener('click', () => {
      diamondsInput.value = '';
      priceInput.value = '15';
      calculateLive(diamondsInput, priceInput, resultEl, resultBox, errorMessage);
    });
  }

  document.querySelectorAll('.faq-question').forEach(button => {
    button.addEventListener('click', () => {
      const item = button.closest('.faq-item');
      if (item) item.classList.toggle('open');
    });
  });

  if (contactForm) {
    contactForm.addEventListener('submit', event => {
      event.preventDefault();
      const submitButton = contactForm.querySelector('button[type="submit"]');
      const statusEl = contactForm.querySelector('.form-status');
      if (submitButton) {
        submitButton.textContent = 'Message sent';
        submitButton.disabled = true;
        setTimeout(() => {
          submitButton.textContent = 'Send message';
          submitButton.disabled = false;
        }, 1800);
      }
      if (statusEl) {
        statusEl.textContent = 'Thanks! Your message has been sent.';
      }
      contactForm.reset();
    });
  }

  newsletterForms.forEach(form => {
    form.addEventListener('submit', event => {
      event.preventDefault();
      const statusEl = form.querySelector('.form-status');
      const submitButton = form.querySelector('button[type="submit"]');
      if (submitButton) {
        submitButton.textContent = 'Subscribed';
        submitButton.disabled = true;
        setTimeout(() => {
          submitButton.textContent = 'Subscribe';
          submitButton.disabled = false;
        }, 1800);
      }
      if (statusEl) {
        statusEl.textContent = 'Thanks for subscribing!';
      }
      form.reset();
    });
  });

  const langSelect = document.querySelector('.lang-switcher select');
  if (langSelect) {
    langSelect.addEventListener('change', event => {
      localStorage.setItem('preferredLanguage', event.target.value);
      applyTranslations();
    });
  }

  applyTranslations();

  window.addEventListener('scroll', () => {
    const header = document.querySelector('.site-header');
    if (header) {
      header.classList.toggle('scrolled', window.scrollY > 24);
    }
  });
});

function calculateLive(diamondsInput, priceInput, resultEl, resultBox, errorMessage) {
  const diamonds = Number(diamondsInput.value.trim());
  const price = Number(priceInput.value.trim());
  const isInvalidDiamonds = Number.isNaN(diamonds) || diamonds < 0;
  const isInvalidPrice = Number.isNaN(price) || price < 0;

  if (isInvalidDiamonds) {
    displayError('Enter a valid diamond quantity.', resultBox, errorMessage, resultEl);
    return;
  }

  if (isInvalidPrice) {
    displayError('Enter a valid price per 100 diamonds.', resultBox, errorMessage, resultEl);
    return;
  }

  clearError(resultBox, errorMessage);
  const total = (diamonds / 100) * price;
  const formatted = total.toLocaleString('en-US', { minimumFractionDigits: 2, maximumFractionDigits: 2 });
  resultEl.textContent = `${formatted} DH`;
  resultBox.classList.add('pulse');
  window.clearTimeout(resultBox.pulseTimeout);
  resultBox.pulseTimeout = window.setTimeout(() => resultBox.classList.remove('pulse'), 300);
}

function displayError(message, resultBox, errorMessage, resultEl) {
  if (errorMessage) errorMessage.textContent = message;
  if (resultBox) {
    resultBox.classList.add('error');
  }
  if (resultEl) resultEl.textContent = '0 DH';
}

function clearError(resultBox, errorMessage) {
  if (errorMessage) errorMessage.textContent = '';
  if (resultBox) {
    resultBox.classList.remove('error');
  }
}
